<?xml version="1.0" encoding="UTF-8"?>

<!-- 마이바티스3 Mapper DOCTYPE -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="mypage.dao.face.MyPageDao">
	
	<select id="selectUserAll" parameterType="user.dto.User_table" resultType="user.dto.User_table" >
		SELECT * FROM User_table
		WHERE userno = #{userno }	
	</select>
	
	<update id="updateUserNick" parameterType="user.dto.User_table">
		UPDATE User_table 
		SET usernick = #{usernick }, userphone = ${userphone }, interest = ${interest } 
		WHERE userno = #{userno }
	</update>

</mapper>